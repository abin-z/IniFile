# installed_test

This folder contains a minimal project to test that the `inifile` library
can be used after installation via `find_package`.  

It is used by CI to verify the install target works correctly.